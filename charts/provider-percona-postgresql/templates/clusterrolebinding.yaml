{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "provider-percona-postgresql.fullname" . }}
  labels:
    {{- include "provider-percona-postgresql.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "provider-percona-postgresql.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "provider-percona-postgresql.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
