# Assert the provider created the PerconaPGRestore for the PITR request and
# resolved the PITR-enabled storage to repo1.
apiVersion: pgv2.percona.com/v2
kind: PerconaPGRestore
metadata:
  name: test-restore-to-latest
spec:
  pgCluster: test-backup-instance
  repoName: repo1


