apiVersion: backup.openeverest.io/v1alpha1
kind: Backup
metadata:
  name: pg-src-backup
spec:
  origin:
    type: Instance
    instanceRef:
      name: pg-src
  classRef:
    name: percona-backup-postgresql
  storageRef:
    name: minio-storage
